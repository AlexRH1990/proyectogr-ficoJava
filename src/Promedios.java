public class Promedios {// clase para el cálculo del promedio
    public int calcularPromedio(int p1, int p2, int f, int j, int e) {
        // Calcula el promedio según la fórmula especificada
        return (int) (p1 * 0.4 + p2 * 0.4 + f * 0.1 + j * 0.05 + e * 0.05);
    }
}
